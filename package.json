{
  "scripts": {
    "lint": "npm --prefix apps/web run lint",
    "type-check": "npm --prefix apps/web run type-check",
    "web:lint": "npm --prefix apps/web run lint",
    "web:type-check": "npm --prefix apps/web run type-check",
    "web:format": "npx --prefix apps/web prettier . --write",
    "web:format:check": "npx --prefix apps/web prettier . --check"
  },
  "dependencies": {
    "@radix-ui/react-switch": "^1.2.6"
  }
}
